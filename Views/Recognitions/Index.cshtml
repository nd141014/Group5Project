@model IEnumerable<Group5Project.Models.Recognition>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Employee.fullEmployeeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RecognitionValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.recognitionDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.recognitionPoints)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RecognizeeEmployeeID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.fullEmployeeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RecognitionValue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.recognitionDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.recognitionPoints)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RecognizeeEmployeeID)
        </td>
        <td>
           
            @Html.ActionLink("Details", "Details", new { id=item.recognitionId }) 
            
        </td>
    </tr>
}

</table>
