@model IEnumerable<Group5Project.Models.CoreValues>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.coreValueName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.coreValueName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.coreValueId }) |
            @Html.ActionLink("Details", "Details", new { id=item.coreValueId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.coreValueId })
        </td>
    </tr>
}

</table>


